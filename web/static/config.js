// EngLISP Client Frontend Configuration
// -------------------------------------------------------------
// When deploying the frontend separately (e.g., to Vercel or Netlify),
// set the URL of your live hosted backend API server here.
// Example: window.ENG_LISP_API_URL = "https://englisp-api.onrender.com";
//
// Leave as an empty string ("") if the backend server is hosting 
// the static files directly (relative paths will be used).
window.ENG_LISP_API_URL = "https://api.englisp.com";
